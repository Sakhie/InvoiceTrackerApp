import { InvoiceCustomerPipe } from './invoice-customer.pipe';

describe('InvoiceCustomerPipe', () => {
  it('create an instance', () => {
    const pipe = new InvoiceCustomerPipe();
    expect(pipe).toBeTruthy();
  });
});
